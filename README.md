# Proxy Server

A self hosted proxy server to give your serverless functions a static IP for external API that need whitelisted IPs

## Installation

git clone proxy
cd proxy
npm install
pm2 start main.js

